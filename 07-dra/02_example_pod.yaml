apiVersion: v1
kind: Pod
metadata:
  name: dra-test-pod
spec:
  resourceClaims:
    - name: gpu
      resourceClaimTemplateName: gpu-claim-template
  containers:
    - name: test
      image: busybox
      command: ["echo", "DRA claim worked"]
      resources:
        claims:
          - name: gpu
  restartPolicy: Never
